{
  "name": "tmc-husband-firebase-rules",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/firestore.rules.test.js",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-tmc-husband-local \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.24.0"
  }
}
