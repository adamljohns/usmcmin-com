// Copy to tmc-husband-firebase.config.js only after a real Firebase project is approved.
// Firebase web config is public metadata, but no real or fabricated values belong in this template.
globalThis.TMC_HUSBAND_FIREBASE_CONFIG = Object.freeze({
  enabled: false,
  firebaseConfig: null
});
