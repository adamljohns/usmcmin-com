# RESOLUTE Citizen Scorecard — Python dependencies
# Target: Python 3.11+
#
# The vast majority of the build scripts use only the standard library
# (json, pathlib, datetime, subprocess, re, collections, argparse, urllib).
# The packages below are the only third-party imports in the tree:

PyYAML>=6.0        # build-og-images.py (config/front-matter parsing)
Pillow>=10.0       # build-og-images.py, build-webp.py (OG image + WebP generation)
GitPython>=3.1     # enrich/fix scripts that inspect git history
